{
  "buildCommand": "npx expo export -p web",
  "outputDirectory": "dist",
  "devCommand": "npx expo start --web",
  "cleanUrls": true,
  "framework": "static",
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/"
    }
  ]
}
